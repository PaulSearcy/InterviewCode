import React from 'react';
import { Link } from 'react-router-dom';

const Main = () => {
    return (
        <Link to="/date" className="mui-btn mui-btn--primary" > Date Picker </Link >
    )
};

export default Main;